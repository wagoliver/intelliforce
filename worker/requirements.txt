# =============================================================================
# IntelliForce Worker — Python dependencies
# =============================================================================

# --- Core async ---
uvloop==0.19.0

# --- Web framework (API) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.9.2
pydantic-settings==2.5.2

# --- Database ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9   # driver sync usado pelo Alembic
alembic==1.13.3
pgvector==0.3.6

# --- ClickHouse ---
clickhouse-driver==0.2.9
clickhouse-connect==0.8.18

# --- Redis (queue + streams + cache) ---
redis[hiredis]==5.0.8

# --- Scheduler ---
apscheduler==3.10.4
croniter==3.0.3   # calcula next_run de uma cron expression

# --- Auth ---
passlib[argon2]==1.7.4
python-jose[cryptography]==3.3.0
python-multipart==0.0.10

# --- Utilities ---
ulid-py==1.1.0
python-dotenv==1.0.1
structlog==24.4.0
httpx==0.27.2
tenacity==9.0.0  # retry com backoff
pyyaml==6.0.2    # parse de frontmatter de SKILL.md / agent.md
cryptography==43.0.3   # Fernet pra Vault — encrypt/decrypt de secrets

# --- LLM provider (abstração OpenAI-compatible) ---
openai==1.51.0  # cliente OpenAI-compatible serve pra LM Studio

# --- Observability (preparado pra futuro) ---
# langfuse==2.50.0
# opentelemetry-api==1.27.0
