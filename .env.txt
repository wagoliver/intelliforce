# =============================================================================
# IntelliForce — Configurações de ambiente
# =============================================================================
# Copie este arquivo pra .env e ajuste os valores antes de subir.
# Pra setup inicial: ./scripts/bootstrap.sh
# =============================================================================

# -----------------------------------------------------------------------------
# Aplicação
# -----------------------------------------------------------------------------
APP_ENV=development                    # development | staging | production
LOG_LEVEL=INFO                         # DEBUG | INFO | WARNING | ERROR


# -----------------------------------------------------------------------------
# Web Push (VAPID) — necessário pro push funcionar
# -----------------------------------------------------------------------------
VAPID_PUBLIC_KEY=BARZV2pT96swd8-w8CbytUsfSqelkgJH5ytHCyMVzPtgtA6xVmM1BLDySl5yVMk9pw2EJYERZkeYhN--kJJV6Y0
VAPID_PRIVATE_KEY=_kgTIQly-mbcbvVFtN0X71pIieNQlXKwvmP1hsLxfK4
VAPID_SUBJECT=mailto:admin@arctica.com.br

# -----------------------------------------------------------------------------
# Timeout do OpenCode em 1h (você pediu antes)
# -----------------------------------------------------------------------------
OPENCODE_TIMEOUT_SECONDS=3600





# -----------------------------------------------------------------------------
# PostgreSQL — estado transacional
# -----------------------------------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=intelliforce
POSTGRES_USER=intelliforce
POSTGRES_PASSWORD=change-me-in-production

# URL completa derivada dos valores acima (não precisa editar normalmente)
DATABASE_URL=postgresql+asyncpg://intelliforce:change-me-in-production@postgres:5432/intelliforce

# -----------------------------------------------------------------------------
# ClickHouse — eventos verbosos e analytics
# -----------------------------------------------------------------------------
CLICKHOUSE_HOST=clickhouse
CLICKHOUSE_PORT=9000
CLICKHOUSE_HTTP_PORT=8123
CLICKHOUSE_DB=intelliforce_audit
CLICKHOUSE_USER=intelliforce
CLICKHOUSE_PASSWORD=change-me-in-production

# -----------------------------------------------------------------------------
# Redis — fila, cache, event bus (Streams)
# -----------------------------------------------------------------------------
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# LM Studio — provedor de LLM
# -----------------------------------------------------------------------------
# host.docker.internal aponta pro Mac/Windows host onde o LM Studio roda.
# Em Linux, troque por IP da rede ou use --add-host no compose.
LMSTUDIO_BASE_URL=http://host.docker.internal:1234/v1
LMSTUDIO_API_KEY=sk-lm-niWo7I8x:2Zq13qurwi2i33kEyHPU
LMSTUDIO_DEFAULT_MODEL = qwen3.6-27b-mtp
LMSTUDIO_MAX_TOKENS=262144
LMSTUDIO_CONTEXT_LENGTH=132144

# -----------------------------------------------------------------------------
# OpenCode — runtime de agentes
# -----------------------------------------------------------------------------
OPENCODE_CONFIG_PATH=/opencode-runtime
OPENCODE_TIMEOUT_SECONDS=3600          # timeout por invocação CLI

# -----------------------------------------------------------------------------
# Auth — JWT
# -----------------------------------------------------------------------------
JWT_SECRET=6271d249f30fbf7dcd69894143904b4a2c717216b15964138be1237ec5adb749
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# -----------------------------------------------------------------------------
# Vault — cofre de credenciais
# -----------------------------------------------------------------------------
# Master key Fernet (urlsafe-base64 de 32 bytes). Gere via:
#   docker compose exec worker python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ⚠️  Se essa key vazar, todos os secrets vazam. Backup seguro obrigatório.
# ⚠️  Trocar a key invalida todos os secrets existentes (eles ficam ilegíveis).
VAULT_MASTER_KEY=sRhD7ZUU6grIWQYgrfI7-DSJ9ZcfJhLl5C0Yow00cBw=

# -----------------------------------------------------------------------------
# Admin inicial — primeiro usuário criado automaticamente vira admin
# -----------------------------------------------------------------------------
ADMIN_EMAIL=admin@arctica.com.br
ADMIN_PASSWORD=BpMTgaR1JzrHCG035s5e                       # se vazio, é gerado pelo bootstrap

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# -----------------------------------------------------------------------------
# Worker
# -----------------------------------------------------------------------------
WORKER_CONCURRENCY=3                  # quantas tarefas em paralelo por worker
WORKER_POLL_INTERVAL_SECONDS=1

# Token JWT longo da service account worker-internal — gere via:
#   docker compose exec worker python -m intelliforce.scripts.gen_worker_token
WORKER_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwZjE5N2Y4Ny1iODBiLTQ2NzYtYmViNi04MGYxNjI2YTkxOGQiLCJleHAiOjE4MDk0ODI4NDAsImlhdCI6MTc3Nzk0Njg0MCwidHlwZSI6ImFjY2VzcyIsImlzX3NlcnZpY2UiOnRydWUsInJvbGUiOiJzZXJ2aWNlIn0.fjQ0D2sb0uOaYJ2605n6hOdtLmqFamSpjJeaR_egbpY
INTELLIFORCE_INTERNAL_API_URL=http://api:8000

# -----------------------------------------------------------------------------
# Debug — eventos crus do OpenCode
# -----------------------------------------------------------------------------
# Ativa log de cada evento NDJSON do CLI (truncado em 500 chars).
# Útil pra diagnosticar o que o modelo está emitindo (reasoning chunks, tool
# calls com shape inesperado, etc.). Desliga colocando =0 ou removendo.
OPENCODE_LOG_RAW_EVENTS=1